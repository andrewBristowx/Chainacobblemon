name: Build Chainacobblemon 0.3.0-alpha.7 vanilla geometry refresh

on:
  push:
    branches:
      - release/0.3.0-alpha.7
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install image dependency
        run: python -m pip install --disable-pip-version-check pillow

      - name: Restore exact Chainacobblemon alpha.5 source
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euxo pipefail
          curl -fL --retry 3 --retry-delay 2 \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            'https://api.github.com/repos/andrewBristowx/Chainacobblemon/actions/artifacts/9271582556/zip' \
            -o /tmp/alpha5-artifact.zip
          mkdir -p /tmp/alpha5-artifact /tmp/chainacobblemon
          unzip -q /tmp/alpha5-artifact.zip -d /tmp/alpha5-artifact
          srczip="$(find /tmp/alpha5-artifact -name 'Chainacobblemon-0.3.0-alpha.5-source.zip' -print -quit)"
          test -n "$srczip"
          unzip -q "$srczip" -d /tmp/chainacobblemon

      - name: Reapply alpha.6 fixes and integrations
        shell: bash
        run: |
          set -euxo pipefail
          python ci/alpha6/apply_alpha6.py /tmp/chainacobblemon
          grep -q 'mod_version=0.3.0-alpha.6+1.21.1' /tmp/chainacobblemon/gradle.properties

      - name: Apply alpha.7 vanilla geometry refresh
        shell: bash
        run: |
          set -euxo pipefail
          python ci/alpha7/apply_alpha7.py /tmp/chainacobblemon
          grep -q 'mod_version=0.3.0-alpha.7+1.21.1' /tmp/chainacobblemon/gradle.properties
          grep -q 'new ArmorItem(' /tmp/chainacobblemon/src/main/java/com/andrewbristowx/chainacobblemon/registry/ModRegistries.java
          ! grep -q 'registerChainaArmorRenderer' /tmp/chainacobblemon/src/client/java/com/andrewbristowx/chainacobblemon/client/ChainacobblemonClient.java
          test -s /tmp/chainacobblemon/src/main/resources/assets/chainacobblemon/textures/item/gacha_ticket.png
          test -s /tmp/chainacobblemon/src/main/resources/assets/chainacobblemon/textures/item/chaina_special_banner_ticket.png
          test -s /tmp/chainacobblemon/src/main/resources/assets/chainacobblemon/textures/item/treasure_gacha_ticket.png

      - name: Compile Chainacobblemon alpha.7
        working-directory: /tmp/chainacobblemon
        shell: bash
        run: |
          set -euxo pipefail
          sed -i 's/\r$//' gradlew
          chmod +x gradlew
          ./gradlew clean build -x test --no-daemon --stacktrace

      - name: Verify alpha.7 jar and integrated systems
        shell: bash
        run: |
          set -euxo pipefail
          src=/tmp/chainacobblemon
          jar="$(find "$src/build/libs" -maxdepth 1 -type f -name '*.jar' ! -name '*-sources.jar' -print -quit)"
          test -n "$jar"
          unzip -p "$jar" fabric.mod.json | grep -q '0.3.0-alpha.7+1.21.1'
          unzip -l "$jar" | grep -q 'com/emiprotecciones/EmiProtecciones.class'
          unzip -l "$jar" | grep -q 'com/andrewbristowx/cobbleeventalerts/CobbleEventAlerts.class'
          unzip -p "$jar" assets/chainacobblemon/geo/chaina_gacha_machine.geo.json | grep -q 'chaina_bell'
          unzip -l "$jar" | grep -q 'assets/chainacobblemon/textures/item/gacha_ticket.png'
          unzip -l "$jar" | grep -q 'assets/chainacobblemon/textures/item/chaina_special_banner_ticket.png'
          unzip -l "$jar" | grep -q 'assets/chainacobblemon/textures/item/treasure_gacha_ticket.png'
          unzip -l "$jar" | grep -q 'assets/chainacobblemon/textures/item/chaina_sword.png'
          unzip -l "$jar" | grep -q 'assets/chainacobblemon/textures/models/armor/chaina_layer_1.png'
          mkdir -p /tmp/release
          cp "$jar" '/tmp/release/Chainacobblemon-0.3.0-alpha.7+1.21.1.jar'
          (cd "$src" && zip -qr '/tmp/release/Chainacobblemon-0.3.0-alpha.7-source.zip' . -x '.gradle/*' 'build/*')
          sha256sum \
            /tmp/release/Chainacobblemon-0.3.0-alpha.7+1.21.1.jar \
            /tmp/release/Chainacobblemon-0.3.0-alpha.7-source.zip \
            > /tmp/release/SHA256SUMS-0.3.0-alpha.7.txt

      - uses: actions/upload-artifact@v4
        with:
          name: Chainacobblemon-0.3.0-alpha.7
          path: /tmp/release/*
