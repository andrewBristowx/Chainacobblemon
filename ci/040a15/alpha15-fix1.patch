--- a/src/main/java/com/andrewbristowx/chainacobblemon/hologram/HologramViewerTextService.java
+++ b/src/main/java/com/andrewbristowx/chainacobblemon/hologram/HologramViewerTextService.java
@@ -103,7 +103,7 @@
                 String fingerprint = entry.text() + "|" + viewerText.getString() + "|" + entry.color();
                 CacheValue previous = LAST_SENT.get(key);
                 if (previous != null && previous.fingerprint().equals(fingerprint)
-                        && ticks - previous.sentTick() < PERIODIC_REFRESH_TICKS) {
+                        && ticks - previous.tick() < PERIODIC_REFRESH_TICKS) {
                     continue;
                 }
